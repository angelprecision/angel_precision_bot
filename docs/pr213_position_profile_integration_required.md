# Position profile integration still required

This branch opens the PR 213 slot and adds first-pass modules. The next amendment must integrate these components into `ap/position_score_profile.py` after PR 209A and PR 212 settle:

- `price_stacking`
- `news_earnings_context`

Until that integration and full tests are added, this PR remains HOLD.
