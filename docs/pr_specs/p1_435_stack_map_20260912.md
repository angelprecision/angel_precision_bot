# Angel Precision — Canonical 435 Replacement Stack (2026-09-12)

## Status

This document is the repository-level map for replacing historical PR #435 without reviving its broad branch.

Historical #435 remains **DO NOT MERGE WHOLESALE**. Its useful responsibilities are being recut into narrow PRs against current main.

Current main at map creation:
`846525a2ee798071bb0079078f0aec871c415677`

## Canonical stack

### 435-A — PR #614 — MERGED

Purpose: exact point-in-time market-data foundation.

Owns:
- exact BREACH as-of from `trigger_crossed_at`;
- exact completed 5m / 15m evidence;
- completed 1h / 4h aggregation;
- bounded daily history;
- provider-vs-frozen source authority;
- authoritative vs stale / missing / not-due coverage;
- zero current-quote contamination for BREACH.

Does not own structural interpretation or entry policy.

### 435-B — PR #615 — ACTIVE / HARD HOLD

Purpose: observe-only market-structure brain.

Owns:
- canonical 4H / 1H FVG geometry and lifecycle;
- stable zone IDs;
- aligned/opposing FVG relationship;
- price position `inside / above / below / boundary`;
- exact VI or explicit MISSING;
- 5m / 15m wick-vs-body penetration;
- >=50% body-through + body/range strength;
- strong-break observation;
- pullback / reclaim / re-breach sequence;
- explicit regime or UNKNOWN;
- setup archetype and observe-only posture.

Required before merge:
- rebase onto post-#614 main;
- consume exact #614 `data_sources["candles"]` shape;
- use #614 `as_of`, never worker collection time, for BREACH evidence;
- use candle completion boundary for breach-straddling post-breach sequencing;
- preserve #614 coverage/authority semantics;
- prove exact #614 -> #615 integration;
- remain `observe_only=true` / `affected_eligibility=false`.

### 435-C — PR #621 — DURABLE BREACH SNAPSHOT ADAPTER / HARD HOLD

Purpose: adapt `#614 PIT + #615 structure` into the durable snapshot substrate already merged by #327/#330.

Must reuse:
- `ap_intelligence_snapshots`;
- `ap_intelligence_jobs`;
- existing snapshot store;
- existing BREACH phase identity and persistence;
- existing parent PRETRIGGER/PREOPEN relationship.

Must prove exact binding of:
- client_id;
- execution_mode;
- signal_id;
- canonical_signal_id;
- local_order_id;
- lifecycle/materialization generation when present;
- trigger_crossed_at;
- exact PIT as-of;
- #615 schema/model version;
- structure input hash / output hash.

Restart/replay must resolve the same snapshot identity and same frozen evidence from the same durable facts.

No second table, worker, persistence system, or identity model.

### 435-D — PR #622 — OBSERVE-ONLY RUNTIME BRIDGE / HARD HOLD

Purpose: reuse #330's existing BREACH dispatch seam and enrich the existing BREACH snapshot with #614/#615 evidence.

Critical latency invariant:

`confirmed BREACH -> cached/frozen evidence lookup -> #615 freeze -> existing BREACH snapshot`

must never become:

`confirmed BREACH -> synchronous provider/history fetch -> rebuild -> wait -> selector`

Rules:
- only already-available/cached/frozen evidence may be consumed synchronously;
- cache miss / unavailable / stale intelligence becomes UNKNOWN/MISSING;
- telemetry failure is fail-soft;
- existing trade path continues unchanged;
- no WAIT/REARM/TERMINAL authority;
- no watcher ownership change;
- no selector delay/gate;
- no broker gate;
- no order/position/proof/queue mutation.

### 435-E — PR #623 — REPLAY / EVIDENCE CLOSURE / HARD HOLD

Purpose: prove the replacement stack describes production reality before any policy authority is added.

Required incident fixtures:
- NOW;
- QQQ;
- HOOD;
- LULU;
- valid positive/negative controls;
- wrong-client / wrong-mode / wrong-generation controls.

Per opportunity capture:
- exact frozen PIT candles;
- relevant FVG identity/relationship;
- first breach;
- wick/body acceptance;
- strong-break state;
- pullback/reclaim/re-breach sequence;
- actual post-breach underlying path;
- uninterrupted vs restart/recovery parity.

Closure condition:
If #614 + #615 + existing #327/#330 machinery reproduce these incidents deterministically with zero money-path change, historical monolith #435 is considered replaced and must never be merged wholesale.

## Post-435 stack

### PR #436 — SURGICAL RECUT ONLY

Purpose: deterministic timing policy over frozen structure.

Input:
`frozen BREACH structure`

Output candidates:
`READY / WAIT / REARM / TERMINAL`

No universal `second touch = buy` rule.
No promotion to LIVE without downstream evidence.

#620 was closed as redundant because its proposed FVG-entry-admission ownership belongs in this #436 recut.

### PR #437 — SELECTED CONTRACT EVIDENCE

After selector truth exists and before broker POST, freeze exact selected-contract evidence:
- OCC;
- BID / ASK / executable BUY authority;
- spread;
- delta / gamma / theta / IV;
- OI / option volume;
- DTE;
- qty / debit;
- premium chase;
- exact client/mode/order/generation identity.

First implementation remains observe-only.

### PR #577 — PATH / OUTCOME CAPTURE

Rebase and audit existing implementation after canonical upstream evidence exists.

Capture:
- MFE / MAE;
- time-to-green;
- submit/fill timing;
- reclaim/re-breach path;
- actual realized result;
- prospective counterfactual evidence only when a shadow decision existed contemporaneously.

### PR #443 — EXACT ATTRIBUTION

Join:
`BREACH snapshot + CONTRACT_SELECTED + actual path + canonical proof`

No fuzzy ticker/time joins.

### PR #438 — PROMOTION GATE

Promotion sequence:
`observe-only -> PAPER authoritative -> LIVE shadow -> explicitly approved limited LIVE`

Compare expectancy, win rate, average win/loss, fill rate, MFE/MAE, time-to-green, skipped winners, avoided losers, concentration, attribution coverage, and uncertainty.

## Separate intraday discovery stack

This stack is independent from the #435 intelligence replacement.

### PR #617 — forming-bar state
Deterministic incremental 5m/15m/30m/60m session-anchored bars for a bounded highly liquid ticker universe.

### PR #618 — pure setup classifier
Pure classification only: `1 / 2U / 2D / 3`, with developing/completed setup detection such as 2-3-2.

### PR #619 — candidate scanner
Bounded orchestration + deterministic deduplication from forming state into observe-only intraday candidates.

Promotion of an intraday candidate into the existing signal/watcher pipeline is a separate future PR and must not be smuggled into #617/#618/#619.

## Global engineering rules

1. Narrow PRs only.
2. One authority owner per responsibility.
3. No duplicate snapshot, scanner, watcher, selector, broker, or proof subsystem.
4. No synchronous full-history rebuild in entry hot paths.
5. Unknown/missing intelligence is not adverse truth.
6. Observe-only stages cannot mutate eligibility.
7. Exact client/mode/signal/order/generation identity is mandatory across joins.
8. No current worker-time information may rewrite frozen decision-time evidence.
9. Every promotion of authority requires replay + exact attribution + explicit release gate.
10. If implementation needs broader production scope than the PR contract, STOP and re-audit rather than silently expanding.
