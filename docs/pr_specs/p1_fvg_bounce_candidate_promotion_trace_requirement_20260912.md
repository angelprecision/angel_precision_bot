# Implementation Trace Requirement

Before production implementation, trace the exact then-current path:

`FVG_HOLD_BOUNCE classifier result -> canonical promotion identity -> existing candidate/signal representation -> watcher/materialization owner -> selector boundary`

If implementation needs a new execution subsystem or broad master-control/watcher/selector changes, stop and keep the PR on HARD HOLD.
