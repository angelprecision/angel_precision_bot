# Implementation Trace Requirement

Before production implementation, trace the exact then-current path:

`#622 runtime intelligence view -> #615/#621 authoritative FVG evidence -> pure FVG_HOLD_BOUNCE classifier -> observe-only output`

If implementation needs direct watcher, selector, broker, order, position, or proof access, stop and keep the PR on HARD HOLD.
